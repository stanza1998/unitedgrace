import { motion } from "framer-motion"
import react from "react"

export const Contact = () => {
    return (
        <motion.div
            initial={{ y: "100vw" }}
            animate={{ y: 0 }}
        >

            contact
        </motion.div>
    )
}